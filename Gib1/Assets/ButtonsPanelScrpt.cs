using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ButtonsPanelScrpt : MonoBehaviour
{
    public Button AccBtn;
    public Button InvBtn;

    // Start is called before the first frame update
    void Start()
    {
//        AccBtn.transform.parent = this.transform.parent;
//        InvBtn.transform.parent = this.transform.parent;
//        Vector3 pos = AccBtn.transform.localPosition;
//        pos.x = pos.x+32f;
//        AccBtn.transform.localPosition = pos;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
